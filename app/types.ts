// Navigation param types for React Navigation
export type RootStackParamList = {
  habits: { focusInput: boolean } | undefined;
  index: { today: boolean } | undefined;
  // Add other routes here as needed
};
